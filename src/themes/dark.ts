import { DefaultTheme } from "styled-components";

const darkColors = {
  textColor: "#ffffff",
  background: "#171717",
  borderColor: "#ffffff",
  hoverBackground: "#ffffff",
  hoverTextColor: "#444444",
};

const dark: DefaultTheme = {
  colors: darkColors,
};

export { dark, darkColors };
